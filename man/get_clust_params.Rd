% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{get_clust_params}
\alias{get_clust_params}
\title{shows statistical parrameters of mclust model: mean and variance and proportion for each gaussian (cluster)}
\usage{
get_clust_params(model)
}
\arguments{
\item{model}{mclust model object}
}
\value{
dataframe with  mean, variance & proportion for each gaussian (cluster)
}
\description{
shows statistical parrameters of mclust model: mean and variance and proportion for each gaussian (cluster)
}
\examples{
file_name <- system.file("extdata", "BBB_frag_contributions.txt", package = "rspci")
df <- load_data(file_name)
dx <- filter(df, FragID == "OH (aliphatic)", Model == "consensus", Property == "overall")
m <- clust(dx$Contribution, dx$MolID)
par <- get.params(m)
}
